# Client-specific environment variables for Vite
# These are exposed to the browser, so don't put secrets here!

VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_API_BASE_URL=/api
